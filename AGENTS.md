# AGENTS.md

## 项目

Codehead 是一个 Coding Agent 项目，用于探索和实现现代 Coding Agent Harness 背后的核心工程机制。

项目采用增量方式开发。优先选择简单、明确、可理解的实现，不要过早引入抽象。

## 开发环境

```
conda activate codehead
```

## 工程原则

* 修改应聚焦当前任务，不要擅自扩大范围。
* 优先编写清晰、直接的 Python 代码，避免不必要的抽象。
* 除非能够解决明确存在的问题，否则不要引入新的框架或依赖。
* 除非任务明确要求改变，否则保持现有行为。
* 引入新的项目结构或约定之前，优先遵循仓库中已有的结构和约定。
* 行为发生变化时，应根据需要新增或更新测试。

## 架构

不要假设当前架构已经定型。

涉及架构修改时：

* 修改前先理解相关执行链路；
* 优先采用能够解决当前问题的最小设计；
* 不要仅仅因为成熟 Coding Agent 使用了某种机制，就复制其复杂度。

当任务涉及已有文档明确记录的系统边界或设计决策时，再查阅相关架构文档。

## 验证

优先使用仓库已有的测试、lint、类型检查和其他验证方式。

先运行与本次修改最相关的检查，再根据修改范围和风险决定是否执行更广泛的验证。

没有实际运行的检查，不得声称已经通过。

## 文档

持久化文档只记录跨任务仍然有价值的信息。

不要仅仅为了遵循流程而创建架构文档、ADR、执行计划或其他过程性文件；只有当变更本身确实需要时才创建。
